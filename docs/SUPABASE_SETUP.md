# Supabase setup (Phase 5)

## What is (and is not) integrated

| Piece | Status |
|--------|--------|
| **Expo app** | Uses **`@supabase/supabase-js`** with URL + anon key from **`app.config.js`**, **`supabase.local.json`**, or **`EXPO_PUBLIC_*` env**. It talks to the **already-provisioned** API only. |
| **SQL migrations in this repo** | Files under **`supabase/migrations/`** are the **schema source of truth**. They are **not** executed by the mobile app or by Expo at build time. |
| **Your Supabase project** | Tables, RLS, and triggers exist **only after** you apply those migrations (see below). Until then, sign-in may work but **queries to `profiles`, `treatments`, etc. will fail** (missing relation / RLS). |

So: **the client is integrated; the database schema is integrated only after you push or paste the SQL** into the project you pointed the app at.

---

## Team dev project (optional reference)

For integration testing you can use the shared dashboard: [Supabase project `pexgbpnsuavlaejwgxnx`](https://supabase.com/dashboard/project/pexgbpnsuavlaejwgxnx). API host pattern: `https://pexgbpnsuavlaejwgxnx.supabase.co` — paste **URL** and **anon key** into `app.config.js` / `supabase.local.json` / env as usual (**never commit** service role keys or user passwords).

---

## Apply schema to your Supabase project

Use **one** of these approaches (same end state if done correctly).

### Option A — SQL Editor (no CLI)

1. Open your project → **SQL** → **New query**.
2. Paste and run **[`supabase/migrations/001_phase5_core.sql`](../supabase/migrations/001_phase5_core.sql)** (profiles, medical_profiles, treatments, providers, RLS, `auth.users` → `profiles` trigger).
3. Paste and run **[`supabase/migrations/002_reference_catalogs.sql`](../supabase/migrations/002_reference_catalogs.sql)** (catalog tables, RLS, seed rows).

### Option B — Supabase CLI (`db push`)

Requires [Supabase CLI](https://supabase.com/docs/guides/cli) (or `npx supabase` / devDependency in `package.json`).

```bash
npx supabase login
npx supabase link --project-ref YOUR_PROJECT_REF   # Dashboard → Project Settings → General → Reference ID
npm run db:push                                      # applies pending files in supabase/migrations/
```

`supabase/config.toml` was generated by **`supabase init`** for local tooling; **`project_id`** here is the **local label**, not your cloud ref — **`link`** stores the remote association separately (see CLI docs).

**If you already applied Option A manually**, the CLI migration history may not match; use **`supabase migration list`** / **`repair`** per [Supabase migration docs](https://supabase.com/docs/guides/deployment/database-migrations), or keep using the dashboard only.

---

## After schema exists

1. **Auth:** Authentication → Providers → enable **Email** (disable email confirmation for faster dev if you want instant sessions).
2. **Env:** Copy [`.env.example`](../.env.example) to `.env` at the repo root, set `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY`, then restart `npx expo start` (use `--clear` if the bundle cached old config).

The app uses **Expo SecureStore** (native) / `localStorage` (web) for the Supabase session.

If env vars are **missing**, the app stays in **stub auth** mode (dev buttons on login).

### Reference catalog admins (`profiles.is_admin`)

RLS in `002_reference_catalogs.sql` allows **insert/update/delete** on catalog tables only when `profiles.is_admin = true` for the signed-in user. To grant admin (SQL Editor or CLI SQL, replace the UUID):

```sql
update public.profiles set is_admin = true where id = 'YOUR_AUTH_USER_UUID';
```

After that, **Settings → Catalog admin** appears in the Expo app so you can manage rows without the service role.
